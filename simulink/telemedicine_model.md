# Simulink model specification

Create `telemedicine.slx` with masked subsystems for Acquisition, Network, Processing Queue, Quality Branch, Expert Review, Report, and Storage. Feed `simulation_runner` parameters into the model and compare throughput, turnaround, and utilisation with `cost_optimizer`. The source repository deliberately does not fabricate a binary Simulink file.
